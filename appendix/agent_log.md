# Agent Usage Log

| # | Date | Task Given to Agent | Agent Output Summary | Verified By Me? | Errors Found | Correction Made |
|---|------|---------------------|----------------------|-----------------|--------------|-----------------|
| 1 | 2026-02-20 | Set up project scaffolding and propose plan for churn classification coursework | Agent created directory structure, requirements.txt, README, .gitignore, and proposed 7-phase plan | Yes | Three issues: (1) README used `Churn Modelling.xlsx` (space) instead of `Churn_Modelling.xlsx` (underscore); (2) Model set excluded dummy baseline and MLP neural net, limiting diversity; (3) `.gitignore` did not exclude `data/*.xlsx` and README lacked data access instructions | (1) Standardised filename to `Churn_Modelling.xlsx` in README; (2) Revised model set to Dummy, LogReg, HistGradientBoosting, MLP; (3) Added `data/*.xlsx` to .gitignore and "Data access" section to README |
| 2 | 2026-02-20 | Apply Step-0 corrections A, B, C per user review | Updated README (filename consistency + data access section), .gitignore (data/*.xlsx), decision register (revised decision #4, added decision #5), agent log (documented errors) | Yes | I checked README, .gitignore, and decision register and confirmed they match the corrected dataset naming, access instructions, and model set. | None |
| 3 | 2026-02-20 | Implement data access instructions: README with Kaggle link + sanity check, shared data_loader.py with file-existence guard | Created `src/data_loader.py` with `load_churn_data()`, constants (DATA_PATH, KAGGLE_URL, TARGET, ID_COLS), and clear error message when file missing. Updated README "Data access" section with Kaggle link, exact filename/path, sanity-check command, and gitignore note. Updated repo structure listing. Tested both happy path (10 000 rows, 11 cols after dropping IDs) and error path (FileNotFoundError with instructions). | Pending | Pending — I have not executed load_churn_data from the repo root yet. | None |
| 4 | 2026-02-20 | Draft Section 1 markdown (dataset framing, target, metrics, constraints, assumptions, agent-evidence table) with placeholders — no EDA, no modelling | Created `appendix/section1_draft.md` with subsections 1A–1E. Used `[N rows]`, `[churn_rate]` etc. as placeholders. Proposed PR-AUC as primary metric, ROC-AUC secondary, Recall@top-20% operational. Listed 5 constraints and 8 assumptions/limitations. Included agent-vs-verification table with evidence-pointer columns. | Yes | I reviewed section1_draft and confirmed the target, metrics, assumptions, and placeholders are correct and consistent with the dataset access approach. | None |
| 5 | 2026-02-21 | Switch to CC0 Kaggle source, add CSV fallback, update README/.gitignore/appendix | Updated KAGGLE_URL in `data_loader.py` to CC0 source. Added CSV fallback: loader tries XLSX first, then CSV. Updated error message to list both accepted paths. README "Data access" now has CC0 licence note, new Kaggle link, CSV fallback instructions, sanity check uses `load_churn_data()`. `.gitignore` now also excludes `data/*.csv`. Updated `section1_draft.md` Kaggle link + licence note. Decision register entry #6 added. | Yes | I confirmed the CC0 Kaggle link update is recorded in README and decision register, and noted this was later superseded by the final sample plus full CSV setup. | None |
| 6 | 2026-02-21 | Update data source to CSV: filename `customer_churn.csv`, remove XLSX/fallback logic | Rewrote `data_loader.py`: single `DATA_PATH = data/customer_churn.csv`, `pd.read_csv`, removed `DATA_PATH_XLSX`/`DATA_PATH_CSV` dual constants. Updated README "Data access" (CSV instructions, new filename, updated repo-structure listing, updated reproduce comment). `.gitignore` already had `data/*.csv`. Decision register #7 added. | Yes | I confirmed the CSV only filename change is recorded and noted it was later superseded by the final sample plus full CSV setup. | None |
| 7 | 2026-02-21 | Add committed sample CSV + full-download fallback logic | Created `data/sample_customer_churn.csv` (200-row stratified sample, ~20.5% churn). Rewrote `data_loader.py`: full → sample fallback with `warnings.warn`; clear error when neither exists. Updated `.gitignore` to ignore only `data/customer_churn.csv` (sample is committed). Updated README "Data access" with sample + full instructions, repo-structure listing, reproduce comment. Decision register #8. | Yes | I confirmed sample_customer_churn.csv is committed, the full dataset file is ignored, and the loader prefers full then falls back to sample. | None |
| 8 | 2026-02-21 | Update Section 1 draft to reflect sample + full CSV data-access approach | Updated `appendix/section1_draft.md` section 1A: changed file format from XLSX to CSV (`customer_churn.csv`), rewrote "Data access" paragraph to describe committed sample at `data/sample_customer_churn.csv`, full download from Kaggle to `data/customer_churn.csv`, and loader fallback behaviour (full → sample with warning → error). Updated 1E evidence table row for data access setup. README already correct. Decision register #9 added. | Yes | I confirmed Step 1 text and the agent verification table correctly describe the sample plus full dataset workflow and file paths. | None |
| 9 | 2026-02-21 | Task 2: Concise visual EDA — produce EDA plan, notebook-ready code snippets, data-quality checks, and per-plot validation checklist. No modelling. | Delivered four sections: (A) 10-plot EDA plan with the question each plot answers, (B) notebook-ready matplotlib/pandas code snippets for all 10 plots plus a preamble cell using `load_churn_data`, each saving to `outputs/`, (C) 12 data-quality and modelling-pitfall bullets phrased as checks, (D) per-plot validation checklist stating exactly what to verify. All findings left as hypotheses with placeholder numbers — no specific results claimed. No code committed; snippets provided for user to paste into notebook. | Yes | Three issues: (1) preamble assumed `from src import load_churn_data, TARGET` — notebook runs outside repo root so `src` is not importable; (2) preamble did not drop ID columns (RowNumber, CustomerId, Surname), which would pollute numeric analyses; (3) `outputs/` path was hardcoded relative, would save to wrong location if notebook CWD ≠ repo root | Agent rewrote preamble to use standalone constants (`TARGET`, `ID_COLS`, `NUMERIC`, `CATEGORICAL`), added `df.drop` for IDs, and introduced explicit `OUT` path variable. Confirmed no column-name mismatch between user's CSV and repo CSV. |
| 10 | 2026-02-21 | Advise on minimal notebook preamble style for Task 2 EDA: minimum sanity checks, `describe()` vs `describe(include="all")`, risks of over-minimising (verification of step 9 preamble) | Recommended 4 non-negotiable checks before plotting: `df.shape` (full vs sample), `df.dtypes` (type surprises), `df.isnull().sum()` (silent drops), and ID-column drop (correctness). Recommended `df.describe(include="all")` over plain `df.describe()` to surface categorical columns (Geography, Gender). Identified 4 risks of being too minimal. Proposed 3-cell structure. Said NUMERIC/CATEGORICAL lists are optional. | Yes | One error: agent recommended dropping ID columns in the Task 2 preamble, but user correctly noted that ID drop is a data-preparation step belonging in Task 3, not Task 2. Task 2 EDA should work on the raw 14-column dataframe and explicitly name columns per plot to avoid ID pollution. | User rejected ID drop in Task 2 preamble; deferred to Task 3 (data preparation). Accepted `df.describe(include="all")`, `os.makedirs("outputs")`, and minimal imports. Plot cells must explicitly select columns to avoid RowNumber/CustomerId. |
| 11 | 2026-02-21 | User revised Plot 1 (target class balance) code: removed `src` dependency, wrote standalone cell with inline `TARGET = "Exited"`, `df[TARGET].value_counts()`, percentage labels on bars, `os.makedirs("outputs")` inside the cell, saves to `outputs/eda_01_target_balance.png` | User rewrote Plot 1 entirely as standalone code — no `from src import …`, no ID drop (consistent with decision #11). Uses `counts.index.astype(str)` for x-axis, adds `f"{pct:.1f}%"` text above each bar, sets human-readable tick labels ("0 Retained", "1 Churned"), title "Plot 1 Target Class Balance Exited". Reason: user has no `src/` folder on local desktop, so all notebook code must be self-contained. | Pending | | |
| 12 | 2026-02-21 | Draft Section 2 markdown (EDA approach, visual results with placeholders, data-quality checks, planned actions, agent-vs-verification table) | Created `appendix/section2_draft.md` with subsections 2A–2E. 2A lists the 10-plot scope table. 2B provides per-plot subsections each with (i) what to look at, (ii) dataframe validation command, (iii) interpretation template with placeholders. 2C lists 12 data-quality checks as hypotheses with status column. 2D lists 10 planned actions linked to Task 3/4. 2E is the agent-vs-verification table with evidence-pointer placeholders. No numbers claimed — all findings are placeholders for user to fill after running notebook. | Pending | | |
| 13 | 2026-02-21 | User corrected preamble: plain `df.describe()` instead of `df.describe(include="all")` | Agent had recommended `include="all"` to surface categorical columns. User reviewed the output and determined it adds no useful information — Geography and Gender are examined individually in Plots 4–5, so their top/freq/unique rows in describe are redundant. Plain `df.describe()` gives the numeric summary needed. | Yes | Agent error: recommended `include="all"` when it was unnecessary. The categorical summary rows do not inform any downstream decision that the dedicated plots don't already cover. | User reverted to plain `df.describe()`. Decision #11 updated to reflect both corrections (ID drop + describe). |
| 14 | 2026-02-21 | User corrected Plot 3 section in Section 2 draft: removed `NumOfProducts` from "what to look at", `.describe()` validation, and interpretation | Agent had listed `NumOfProducts` in Plot 3 (numeric distributions) but the actual Plot 3 histogram does not include it — `NumOfProducts` is discrete and has its own dedicated Plot 10. The interpretation bullet claiming to show `NumOfProducts` distribution was inconsistent with the plot. | Yes | Agent error: Plot 3 section described `NumOfProducts` as part of the numeric histograms, but the plot does not include it. The interpretation claimed a finding (`NumOfProducts` distribution) that the plot cannot support. | Removed `NumOfProducts` from Plot 3's three subsections (what to look at, validation, interpretation). It remains covered in Plot 10. Decision #14 added. |
| 15 | 2026-02-21 | Consolidate EDA from 10 plots to 6, reorder to match rubric sequence (distributions → missingness → leakage → class imbalance → outliers), rewrite Section 2 draft, provide notebook-ready code snippets | Rewrote `section2_draft.md`: 2A scope table now lists 6 plots (numeric distributions, missingness, correlation heatmap + leakage, target balance, churn by category, boxplots by churn). 2B has 6 subsections. Merged old Geography + Gender plots into one side-by-side Plot 5. Absorbed zero-balance segment (visible in Plot 1 histogram), leakage bar chart (into Plot 3 heatmap + print output), and NumOfProducts-vs-churn (surfaced from `describe()` + heatmap). 2C keeps 12 checks with updated plot references. 2D keeps 10 actions. 2E evidence table updated for 6-plot scope. Code snippets provided in conversation for user to paste into notebook. | Yes | Agent's original 10-plot plan was not concise enough for the rubric requirement. User identified the redundancy. | Consolidated to 6 plots. Decision #15 added. Section 2 draft fully rewritten. Old decisions #10, #14 superseded. |
| 16 | 2026-02-21 | Fix matplotlib deprecation warning in boxplot code; advise on placement of categorical churn-rate plot; check rubric requirements for EDA scope | Identified `labels=` → `tick_labels=` rename needed in `ax.boxplot()` to silence `MatplotlibDeprecationWarning` (deprecated since 3.9, removed in 3.11). User asked whether "Churn rate by categorical features" (Plot 5) belongs under "continuous numeric distributions" heading — advised it does not (bar charts of percentages ≠ distributions of continuous variables). Checked rubric: required items are distributions, missingness, leakage risks, class imbalance, outliers — churn rate by category is not listed. User decided to drop Plot 5 entirely to keep EDA concise. EDA reduced from 6 to 5 plots. | Yes | Agent's 6-plot plan (decision #15) included "churn rate by categorical features" which is not explicitly required by the rubric. Plot was useful but made the EDA section unnecessarily large. | User dropped Plot 5 (churn rate by categorical features). Applied `labels` → `tick_labels` fix in boxplot code. EDA now has 5 plots: (1) numeric distributions, (2) missingness, (3) correlation heatmap + leakage, (4) target class balance, (5) boxplots by churn. |
| 17 | 2026-02-21 | Explore adding categorical bar charts to Plot 1; evaluate whether categorical count bars are meaningful EDA; decide on `df.describe()` vs `df.describe(include="all")` | Provided code for three layout options: (a) separate 1×2 categorical bar chart, (b) combined 1×7 single-row figure with categorical + numeric, (c) combined 2×5 two-row figure. Advised that categorical count bars on their own are not insightful EDA — they show sample composition (e.g. 5,014 males vs 4,543 females) but reveal nothing about churn behaviour. Recommended keeping Plot 1 as numeric-only histograms and letting `df.describe(include="all")` cover categorical composition. | Yes | Agent's assessment was correct: categorical count bars add visual clutter without analytical insight. The existing Plot 5 (churn rate by category) would have been the meaningful categorical visualisation, but it was already dropped (Decision #16). | User agreed: kept Plot 1 as numeric-only, reinstated `df.describe(include="all")` in the notebook preamble to cover categorical features. This reverses the `describe` part of Log #13 / Decision #11. Decision #17 added. Section 2 draft updated: scope table reduced to 5 plots, Plot 5 (churn by category) section removed, old Plot 6 (boxplots) renumbered to Plot 5, all cross-references updated. |
| 18 | 2026-02-22 | Split Plot 3 into separate correlation heatmap (Plot 3) and leakage risk check (Plot 3b); update Section 2 draft code to match user's actual notebook cells | Agent initially combined both concerns (correlation insight + leakage testing) into a single Plot 3 section titled "Correlation Heatmap (+ Leakage Check)". Agent then drafted a split: Plot 3 for heatmap, Plot 3b for leakage with a domain-sense dictionary and formatted unicode output. User replaced both code blocks with their actual notebook code: heatmap uses `ax.imshow` with `RdBu_r` colourmap, annotated cells, and `corr = df[NUMERIC + [TARGET]].corr()`; leakage check re-uses `corr` to extract `corr_with_target`, applies |r| > 0.8 threshold, and prints results. Agent's domain-sense review was dropped — user preferred a simpler threshold-only check. Updated 2A scope table (Plot 3 + 3b), "What was split" paragraph, 2C check #7 reference, 2D action #10 reference, and 2E verification row. | Yes | Agent error: initial combined section was repetitive — the heatmap already showed correlations, and the leakage check re-stated them in a different format. Agent's second draft (split version) included an over-engineered domain-sense dictionary that the user did not want. | User wrote both notebook cells; agent's domain-sense dictionary replaced with simpler threshold check. Section 2 draft updated to reflect user's actual code. Decision #18 added. |
| 19 | 2026-02-22 | Implement Task 3 v1: preprocessing pipeline, stratified split, data validation, entry point script, section 3 draft | Created `src/preprocessing.py` with 60/20/20 split, `HasBalance` engineered feature, and 7 validation checks. Created `src/03_preprocess.py` that saved three output files (split_indices.json, preprocessor.joblib, validation_report.json). Created `appendix/section3_draft.md`. Updated README. Script ran successfully: 10,000 rows, 14 output features, churn rate ~20.4% across all splits. | Yes | Over-engineered: HasBalance feature not needed for minimal rubric-aligned submission; 60/20/20 gives less training data than 70/15/15; saving output files adds clutter when Task 4 can call the same deterministic functions. | User requested revision — see Log #20. |
| 20 | 2026-02-22 | Revise Task 3 to minimal rubric-aligned setup per user's detailed requirements | Rewrote `src/preprocessing.py`: changed split to 70/15/15 (supersedes Decision #1), removed `HasBalance` feature and `ENGINEERED_FEATURES`, added Balance range check to validation, simplified `validate_data()` to print-only (no report dict), replaced `split_class_balance()` with simpler `print_split_balance()`. Rewrote `src/03_preprocess.py`: removed all file saving (no JSON, no joblib), removed joblib import, simplified to 4-step print-only flow. Rewrote `appendix/section3_draft.md` for 70/15/15, no HasBalance, no output files. Removed `outputs/split_indices.json` and `outputs/validation_report.json` from repo. Updated README: 60/20/20 → 70/15/15, removed "save" from 03_preprocess description. Recommended against log1p for Balance (HistGBT is monotonic-invariant, StandardScaler sufficient for LogReg/MLP). | (fill in) | (fill in after reviewing code and running script) | (fill in) |
| 21 | 2026-02-22 | Integrate notebook cells into section 3 draft as a unified plan document | Created first integrated draft of `section3_draft.md` with "How to use this document" preamble. Each section (3A–3D) had three parts: report text, notebook cell, per-section verify checklist. Provided 6 notebook cells (imports, drop IDs, split, validation, class balance per split, pipeline). Added section 3D (Complete Verification Checklist) as a 12-row summary table, and 3E (Agent Plan vs My Verification). | Yes | Three structural issues: (1) validation checks appeared in four places (Cell 4, Cell 5, 3B report table, 3D checklist) — redundant; (2) Cell 2 used `df = df.drop(...)` overwriting the EDA dataframe; (3) report text was separate from corresponding code cells, requiring user to jump between Part 2 and Part 3 | User requested all three fixes — see Logs #22–#24 |
| 22 | 2026-02-22 | Advise on Gender encoding: should user manually encode Male=1, Female=0 instead of OneHotEncoder? | Explained that OneHotEncoder already handles binary encoding. Described the trade-off: current pipeline produces 2 columns (Gender_Female, Gender_Male) which are perfectly collinear, but regularisation absorbs this for LogReg/MLP and tree models are unaffected. Noted `drop="first"` option to get a single column, but recommended against it because `handle_unknown="ignore"` + `drop="first"` can cause edge cases, and keeping both columns aids interpretability in Task 4 feature importance. | Yes | Agent's recommendation was correct — no manual binary encoding needed. | None |
| 23 | 2026-02-22 | Rewrite section 3 draft: remove redundancy, use df_model, add pitfall prints, reduce to 5 cells | Rewrote `section3_draft.md` into three parts: (1) plain-English coding plan, (2) 5 notebook cells, (3) report text. Key changes: replaced `df = df.drop(...)` with `df_model = df.drop(...)` to preserve EDA dataframe; merged Cell 4 (validation) and Cell 5 (class balance) into single Cell 3; removed redundant section 3D (Complete Verification Checklist); removed per-section Verify bullet lists; added `(df_model["Balance"] == 0).mean()` and `df_model["NumOfProducts"].value_counts().sort_index()` as pitfall prints in Cell 3; report text 3B references both prints as modelling pitfalls with interpretation. Cells reduced from 6 to 5. | Yes | Agent correctly implemented all user-specified changes: df_model naming, redundancy removal, pitfall prints, 5-cell structure. | None |
| 24 | 2026-02-22 | Restructure section 3 draft: move report text directly under each corresponding code cell | Removed the three-part layout (coding plan / notebook cells / report text as separate sections). New structure: coding plan at top, then each cell heading followed by its code block and immediately followed by its report text (3B under Cell 3, 3A under Cell 4, 3C under Cell 5). Section 3D (Agent Plan vs My Verification) stays at the bottom. No content changed — only reorganised so code and interpretation read top-to-bottom without jumping. | Yes | Agent correctly reorganised without changing content. | None |
