# Decision Register

| # | Decision | Alternatives Considered | Rationale | Agent Suggested? | I Agreed? | Notes |
|---|----------|------------------------|-----------|-----------------|-----------|-------|
| 1 | Stratified 60/20/20 split | 70/15/15, 80/10/10, k-fold only | 60/20/20 gives a reasonably sized validation set for stable metric estimates and a proper held-out test set; stratification preserves class balance across all three sets | Yes | (fill in) | |
| 2 | Use single sklearn Pipeline with ColumnTransformer | Separate preprocessing scripts, pandas-based transforms | Pipeline prevents train-test leakage by fitting transformers only on training data; ColumnTransformer handles mixed types cleanly | Yes | (fill in) | |
| 3 | Metrics: ROC-AUC, PR-AUC, Recall@top-20% | Accuracy, F1 only | Dataset is imbalanced (~20% churn); PR-AUC is more informative than ROC-AUC for imbalanced data; Recall@top-20% simulates a business scenario of targeting the most likely churners | Yes | (fill in) | |
| 4 | Models: DummyClassifier baseline, Logistic Regression, HistGradientBoostingClassifier, MLPClassifier with early stopping (Random Forest optional) | RF + plain GBM only, XGBoost, SVM | Dummy baseline establishes a no-skill reference. LogReg is a strong linear baseline. HistGradientBoosting is sklearn-native modern boosting (handles categoricals, fast, no extra dependency). MLP with early stopping adds a neural-net perspective. This set spans no-skill → linear → ensemble → neural, meeting coursework breadth requirements. RF excluded from core set to keep report concise but can be added if space allows | Initially agent suggested LogReg, RF, GBM only; user corrected to include dummy baseline and MLP, replace GBM with HistGradientBoosting | (fill in) | **Agent error caught**: original plan excluded dummy baseline and neural nets, limiting model diversity. User corrected. |
| 5 | Exclude data file from repo, provide access instructions in README | Commit the xlsx, use Git LFS | Coursework says "data access instructions (and/or a small sample, if license allows)". Keeping xlsx out of git history avoids bloat and respects potential license constraints. README now documents how to obtain and place the file | Agent had not gitignored xlsx; user requested correction | (fill in) | |
| 6 | Switch dataset source link to CC0-licensed Kaggle page (anandshaw2001/customer-churn-dataset) | Keep original shantanudhakadd link, host data ourselves | The new Kaggle page explicitly lists CC0 (public domain) licence, which provides clearer legal footing for a public repository. The underlying data is the same Bank Customer Churn Modelling dataset. Also added CSV fallback in the loader since some Kaggle downloads provide CSV | User identified the CC0 source and requested the change | (fill in) | |
| 7 | Standardise data filename to `customer_churn.csv` (CSV only, drop XLSX/fallback) | Keep dual XLSX+CSV fallback, keep `Churn_Modelling` name | The CC0 Kaggle download provides a CSV. Using the exact download filename (`customer_churn.csv`) removes a rename step for the user and simplifies the loader to a single `pd.read_csv` call. XLSX support and `openpyxl` dependency are no longer needed for data loading | User identified that the download is CSV and specified the exact filename | (fill in) | |
| 8 | Commit a 200-row stratified sample (`data/sample_customer_churn.csv`); loader falls back to it when full file is absent | Full file only (no sample), embed full dataset in repo | Coursework says "data access instructions (and/or a small sample, if license allows)". CC0 licence permits redistribution. Sample lets scripts run out of the box for a quick preview; `.gitignore` excludes only the full file. Loader warns clearly when using sample so results are not mistaken for final | User specified the approach and filenames | (fill in) | |
| 9 | Align Section 1 draft text with current data-access setup (sample CSV committed, full CSV downloaded, loader fallback) | Leave Section 1 referencing old XLSX-only wording | Section 1 is the coursework submission text seen by the marker. It must accurately describe how data is accessed: sample in repo for quick preview, full CSV from Kaggle for final results, loader reads full first then falls back to sample with a warning. Old wording referenced `Churn_Modelling.xlsx` and a single-file-only approach that no longer matches the repo | User identified the inconsistency and requested the fix | (fill in) | |
| 10 | EDA scope: 10 visual checks (class balance, missingness, distributions, churn rate by category, zero-balance segment, correlation heatmap, boxplots by churn, leakage check) saved to `outputs/` for reproducibility; no modelling | Fewer plots (5–6 only), or add PCA/UMAP projections, or include statistical significance tests | 10 plots cover the key questions for a tabular binary-classification EDA: class balance informs metric choice, distributions inform preprocessing, category breakdowns identify high-churn segments, correlation flags redundancy, boxplots reveal distributional shifts between classes. Saving to `outputs/` ensures plots are reproducible via `run_all.py`. PCA/UMAP deferred as unnecessary for an 11-feature dataset; statistical tests deferred to keep the report concise | Yes — agent proposed the plot list | (fill in) | |
| 11 | Task 2 preamble: minimal imports + `os.makedirs("outputs")` + `df.describe(include="all")`; sanity check stays in Task 1; ID-column drop deferred to Task 3 | Agent recommended `df.describe(include="all")` and ID drop in preamble; user initially reverted to plain `df.describe()`, later reinstated `include="all"` | ID drop belongs in Task 3, not the EDA preamble. Initially `include="all"` seemed redundant when a categorical churn-rate plot was planned (Log #13). After that plot was dropped (Decision #16), `describe(include="all")` became the only place categorical composition is summarised, so it was reinstated (Decision #17) | Agent recommended both `include="all"` and ID drop; **user corrected ID drop**; `include="all"` was initially rejected then reinstated | Yes | **One agent error caught**: ID drop placed too early. `include="all"` was a correct recommendation — user initially rejected it, later agreed after dropping the categorical plot. |
| 12 | Plot 1 code: standalone cell (no `src` imports); inline `TARGET = "Exited"`, `os.makedirs`, `value_counts`, percentage labels, human-readable tick labels, save to `outputs/eda_01_target_balance.png` | Agent's original snippet used `from src import load_churn_data, TARGET` | User has no `src/` folder on local desktop — notebook must be fully self-contained. Each plot cell defines its own constants and column selections inline. This also avoids the import-path issue flagged in log #9 (notebook CWD ≠ repo root). Consistent with decision #11 (no ID drop in Task 2) | User wrote the code; agent had originally relied on `src` imports | Yes | Agent's original Plot 1 snippet would have failed on user's machine due to missing `src/`. User's standalone approach is more portable. |
| 13 | Section 2 draft structure: 2A (EDA approach + 10-plot scope table), 2B (per-plot subsections with validation commands and placeholder interpretations), 2C (12 data-quality checks as hypotheses), 2D (10 planned actions linked to Tasks 3–4), 2E (agent-vs-verification table) | Single combined results section without per-plot validation; shorter check list without status tracking | Matching Section 1 style ensures consistency for the marker. Per-plot validation commands let the user verify every visual against raw dataframe output. Keeping checks as hypotheses (not claims) avoids fabricating results — all numbers are placeholders for the user to fill. Status column in 2C provides an auditable trail of what was confirmed | Yes — agent drafted structure; user specified the deliverable format | (fill in) | All `[placeholders]` must be filled by user after running notebook on full dataset |
| 14 | Plot 3 section: remove `NumOfProducts` from numeric distributions (what to look at, `.describe()` validation, interpretation); it belongs in Plot 10 only | Keep `NumOfProducts` in Plot 3 alongside continuous features | `NumOfProducts` is a discrete variable (values 1–4) examined in its own dedicated Plot 10 (NumOfProducts vs Churn). The actual Plot 3 histogram does not include it, so the Section 2 draft text was inconsistent with the plot. Removing it avoids claiming a finding the plot cannot support | Agent included it in Plot 3 section; **user corrected** | Yes | **Agent error caught**: Plot 3 draft text listed `NumOfProducts` but the plot doesn't show it. |
| 15 | Consolidate EDA to 6 plots in rubric order: (1) numeric distributions, (2) missingness, (3) correlation heatmap + leakage check, (4) target class balance, (5) churn rate by category (Geography + Gender merged), (6) boxplots by churn. Supersedes decision #10 (10-plot scope) | Keep all 10 original plots | 10 plots was not concise — the rubric asks for a "concise visual EDA" covering distributions, missingness, leakage risks, class imbalance, and outliers. 6 plots cover all five areas. Dropped plots were redundant: zero-balance spike visible in Plot 1 histogram; leakage check absorbed into Plot 3 heatmap + print output; NumOfProducts patterns surfaced from `describe()` and heatmap; Geography + Gender merged into one side-by-side figure | Agent proposed original 10-plot plan; **user identified redundancy and requested consolidation + reordering** | Yes | **Agent error caught**: 10-plot plan was over-engineered for a concise EDA. User streamlined to 6. Supersedes decisions #10 and #14. |
| 16 | Drop "Churn rate by categorical features" plot (old Plot 5) from EDA; reduce from 6 to 5 plots. Also fix matplotlib deprecation: `labels=` → `tick_labels=` in boxplot code | Keep all 6 plots and move Plot 5 to a correctly labelled section; keep 6 plots as extra analysis | Rubric requires "distributions, missingness, leakage risks, class imbalance, outliers" — churn rate by category is not listed. User wants EDA section to remain concise. Categorical patterns are still partially visible in the correlation heatmap. Matplotlib fix is a straightforward API rename (deprecated 3.9, removed 3.11) | Agent included churn-rate-by-category in the 6-plot plan (decision #15); **user decided to drop it after checking rubric** | Yes | Supersedes the 6-plot scope in decision #15 (now 5 plots). Remaining plots: (1) numeric distributions, (2) missingness, (3) correlation heatmap + leakage, (4) target class balance, (5) boxplots by churn. All five rubric items covered. |
| 17 | Reinstate `df.describe(include="all")` in notebook preamble; keep Plot 1 as numeric-only histograms; no dedicated categorical count plot | Add categorical bar charts (Geography, Gender) to Plot 1 as a combined 2-row figure; or add a separate categorical count plot | User explored combining categorical bar charts with the numeric histograms (1-row and 2-row layouts) but concluded that categorical count bars are not meaningful EDA on their own — they show sample composition but not churn behaviour. With the categorical churn-rate plot already dropped (Decision #16), `df.describe(include="all")` is the right place to summarise categorical composition. This reverses the `describe` part of Decision #11 (which was originally rejected because dedicated categorical plots existed) | Agent proposed categorical bar charts; **user decided against after evaluating insight value** | Yes | Reinstates `include="all"` that was rejected in Decision #11. Rationale changed: no categorical plot exists, so `describe(include="all")` fills the gap. |
| 18 | Split Plot 3 into two cells: Plot 3 (correlation heatmap for insight) and Plot 3b (leakage risk check with |r| > 0.8 threshold). Drop agent's domain-sense dictionary in favour of simpler threshold-only leakage check | Keep combined "Correlation Heatmap (+ Leakage Check)" as one section; or keep agent's domain-sense dictionary alongside the threshold check | The combined section was repetitive — the heatmap already displays all correlations including with the target, and the leakage text restated the same values. Splitting keeps each cell focused on one concern: Plot 3 is a visual insight tool (multicollinearity, feature relationships), Plot 3b is a systematic leakage test. The domain-sense dictionary was dropped because this dataset has no ambiguous features — all are clearly snapshot-level attributes, so a simple threshold check is sufficient and matches the user's notebook style | Agent combined them; **user identified the repetition and split them**. Agent's second draft included domain-sense review; **user replaced with simpler code** | Yes | Supersedes the leakage portion of decisions #15 and #16 (which described leakage as "absorbed into Plot 3's heatmap + print output"). Plot 3 now covers correlation insight only; Plot 3b covers leakage only. |
| 19 | Task 3 v1: 60/20/20 split, HasBalance feature, save three output files | Keep 60/20/20, keep HasBalance, keep saved outputs | Agent's initial implementation was functional but over-engineered for a minimal rubric-aligned submission | Yes — agent proposed the design | Yes | **Superseded by Decision #20** — user requested minimal revision |
| 20 | Task 3 v2 (minimal): change split to 70/15/15, remove HasBalance, remove all output file saving, add Balance range check, print-only validation | (a) Keep 60/20/20 (Decision #1), (b) Keep HasBalance (Decision #19), (c) Keep saved output files for Task 4, (d) Add log1p for Balance via FunctionTransformer | 70/15/15 gives 7,000 training rows (vs 6,000) for better model fitting while 1,500 per val/test is still adequate for metric estimation. HasBalance removed — models can find the zero split on their own, and minimal approach avoids feature-engineering complexity. Output files removed — `stratified_split()` with seed=42 is deterministic, so Task 4 reproduces the exact same partition by calling the same function. log1p skipped: HistGradientBoosting is invariant to monotonic transforms, and LogReg/MLP already get StandardScaler which handles centering/scaling; adding FunctionTransformer for one column adds pipeline complexity for uncertain gain. Balance range check added to validation since EDA flagged the zero-spike and heavy tail. | Agent proposed v1; **user specified all changes in v2** | (fill in) | **Supersedes Decision #1** (60/20/20 → 70/15/15) and **Decision #19** (v1 → v2). Consistent with Decision #2 (single Pipeline with ColumnTransformer). |
| 21 | Use `df_model` instead of overwriting `df` — preserve the raw EDA dataframe | (a) Overwrite `df = df.drop(...)` as agent initially did, (b) Use `df_raw` and `df_model` as two separate names | `df` is used throughout all Task 2 EDA cells (Plots 1–5, describe, validation). Overwriting it with `df.drop(columns=ID_COLS)` would break re-runnability of EDA cells if the user goes back up in the notebook. `df_model = df.drop(...)` creates a clean modelling dataframe while leaving `df` intact. Simpler than introducing `df_raw` since `df` already exists from the EDA preamble | Agent initially used `df = df.drop(...)`; **user requested `df_model`** | Yes | Consistent with notebook style: EDA cells use `df`, Task 3+ cells use `df_model`. |
| 22 | Keep Gender as OneHotEncoded (2 columns) without `drop="first"` or manual binary encoding | (a) Manual binary encoding Male=1, Female=0, (b) Add `drop="first"` to OneHotEncoder to get single column, (c) Keep both OHE columns (current) | OneHotEncoder already handles the encoding — no manual step needed. Keeping both columns avoids `handle_unknown="ignore"` + `drop="first"` edge cases. Two collinear columns are harmless: regularisation handles it for LogReg/MLP, tree models are unaffected. Both columns in the output improve interpretability for Task 4 feature importance. `drop="first"` would also reduce Geography from 3 to 2 columns, which is less transparent | Agent recommended keeping current setup; **user asked the question, then agreed** | Yes | |
| 23 | Remove redundant validation checklist (old section 3.4); merge all validation into one cell and one report table | (a) Keep separate Cell 4 (validation) + Cell 5 (class balance) + report table 3.2 + checklist 3.4, (b) Keep two of the four | Validation checks appeared in four places: Cell 4 code, Cell 5 code, 3.2 report table, 3.4 checklist table. 3.4 was nearly a verbatim copy of 3.2. Per-section Verify bullet lists also overlapped. One cell + one table is sufficient: the cell produces the output, the report table documents it. Removing duplication makes the draft clearer and shorter | Agent created the redundancy in Log #21; **user identified it and requested consolidation** | Yes | Section 3.4 (checklist) deleted entirely. Per-section Verify bullets deleted. Cells 4+5 merged into single Cell 3. |
| 24 | Add Balance == 0 fraction and NumOfProducts value counts as modelling-pitfall prints in the validation cell | (a) Skip pitfall prints — EDA already noted zero-balance spike and NumOfProducts distribution, (b) Add as separate cell, (c) Embed in validation cell | Both quirks were flagged in EDA (Section 2.7 issues #3 and #6, Section 2.7 actions #3 and #4) but were not explicitly quantified in Task 3. Printing them in the pre-split validation cell documents the exact numbers that enter the pipeline. Report text 3.2 references them as modelling pitfalls with brief interpretation, showing the marker awareness of distributional issues without repeating EDA plots | **User requested both prints** | Yes | Prints are checks 6 and 7 in Cell 3 (validation). Report text 3.1 has two bullet points referencing each. |
| 25 | Section 3 draft structure: report text directly under each corresponding notebook cell, not in a separate Part 3 | (a) Keep three-part layout (coding plan / all cells / all report text), (b) Interleave code and text | Interleaving means the reader sees the code, then immediately reads what to write about it — no jumping between sections. The coding plan stays at the top as a roadmap. Section 3.4 (Agent Plan vs My Verification) stays at the bottom since it spans all cells | Agent initially used three-part layout (Log #21, #23); **user requested interleaving** | Yes | Final cell order: Cell 1 (imports, no text), Cell 2 (df_model, no text), Cell 3 (validation) → 3.1 text, Cell 4 (split) → 3.2 text, Cell 5 (pipeline) → 3.3 text, then 3.4 at bottom. |
| 26 | Task 3 v3 (final structure): `df_raw` + `df_model` naming, two-stage validation (A pre-split integrity + pitfalls, B post-preprocessing checks), 6 notebook cells, no transformed-data table | (a) Keep `df_model` only (Decision #21), (b) Keep single-stage pre-split validation only (v2 draft), (c) Add table of first 5 transformed rows for the marker, (d) Keep 5 cells without post-preprocessing cell | `df_raw` makes naming explicit — `df` (EDA preamble), `df_raw` (preserved copy), `df_model` (identifiers dropped). Two-stage validation covers both data integrity (pre-split) and pipeline correctness (post-preprocessing), matching rubric's "data validation checks" more thoroughly. 6 cells keep each concern isolated. Feature names list (Cell 5) is sufficient — scaled floats in a table are not interpretable at a glance. handle_unknown test with "Atlantis" directly demonstrates OHE safety net | Agent proposed Cell 6 and advised against transformed-data table; **user specified `df_raw` naming, two-stage validation structure, and all constraints** | (fill in) | **Supersedes Decision #21** (`df_model` only → `df_raw` + `df_model`). Adds post-preprocessing validation (Cell 6) not present in any prior decision. |
| 27 | Reinstate categorical churn-rate plot as Plot 4 (4-panel bar chart: Geography, Gender, IsActiveMember, HasCrCard). EDA restored to 6 plots + 1 leakage check. Old Plot 4 (target balance) → Plot 5, old Plot 5 (boxplots) → Plot 6. Output filenames renumbered accordingly | (a) Keep the printed table alternative (agent suggestion), (b) Keep 5-plot scope without categorical analysis (Decision #16), (c) Add churn rates as extra text in describe preamble only | A bar-chart plot is more effective than a printed table — the visual format makes category-level churn-rate differences immediately obvious without reading numbers. `df.describe(include="all")` shows counts and composition but not churn behaviour, so the plot fills a genuine insight gap. Including IsActiveMember and HasCrCard (not just Geography and Gender) gives a complete categorical picture. 6 plots is still concise and all rubric areas remain covered | Agent suggested printed table; **user decided bar chart is clearer and wrote the code** | Yes | **Partially supersedes Decision #16** (which dropped the categorical plot). Decision #17 (`describe(include="all")` for categorical composition) still applies — Plot 4 complements it by showing churn rates, not counts. |
| 28 | Reorder EDA sections to a logical analytical flow and change numbering from letter-based (2A, 2B.1) to decimal (2.1, 2.2). New order: preamble (`df.describe`) → 2.1 Class imbalance → 2.2 Missingness → 2.3 Distributions → 2.4 Categorical churn rates → 2.5 Outliers → 2.6 Leakage risks → 2.7 Data-quality issues and actions → 2.8 Agent verification. Merge old 2C (checks) and 2D (actions) into single Section 2.7 | (a) Keep agent's original ordering: distributions → missingness → correlations → leakage → categories → class balance → outliers, (b) Keep letter-based numbering (2A, 2B.1) | User's ordering follows a more logical analytical flow: class imbalance first (sets evaluation context and metric choice before looking at anything else), then missingness (data cleaning needs before exploring further), then distributions (feature shapes), then categorical rates (segment insights and fairness flags), then outliers (modelling concerns), then leakage as a final sanity check. Decimal numbering (2.1, 2.2) is cleaner and more standard than letter-based (2A, 2B.1). Merging checks and actions into Section 2.7 keeps related content together — issues and their responses belong side by side | **User identified that agent's ordering was not optimal and specified this improved flow** | Yes | **Supersedes all previous section numbering in Section 2.** Mapping: old 2A intro → intro paragraphs; old 2B.5 → 2.1; old 2B.2 → 2.2; old 2B.1 → 2.3; old 2B.4 → 2.4; old 2B.6 → 2.5; old 2B.3+2B.3b → 2.6; old 2C+2D → 2.7; old 2E → 2.8 |
| 29 | Apply decimal numbering to Section 1 (1A–1E → 1.1–1.5) and Section 3 (3A–3D → 3.1–3.4), consistent with Section 2's decimal style | Keep letter-based suffixes for sections 1 and 3; use decimal for section 2 only | Consistent decimal numbering (1.1, 2.1, 3.1) across all three section drafts makes cross-referencing unambiguous and aligns with the convention already established in Decision #28 for Section 2. Internal cross-references and appendix entries updated accordingly. Mapping: 1A→1.1 (Dataset), 1B→1.2 (Target), 1C→1.3 (Metrics), 1D→1.4 (Assumptions), 1E→1.5 (Verification); 3B→3.1 (Data Validation), 3A→3.2 (Split Discipline), 3C→3.3 (Preprocessing Pipeline), 3D→3.4 (Agent Plan) | Agent used letter suffixes throughout; **user requested decimal style to match Section 2** | Yes | **Supersedes all previous section numbering in Sections 1 and 3.** Cross-references updated: "Section 1C" → "Section 1.3" in 1.4; "1A–1E" → "1.1–1.5" in 1.5 table; "3C" internal refs in section3_draft → "3.3"; old 3D/3B/3A/3C labels in verification table → 3.4/3.1/3.2/3.3. Decision #24 note updated: "2C checks, 2D actions" → "Section 2.7". Initial assignment had 3.1=Split, 3.2=Validation which was out of order — corrected in Decision #30. |
| 30 | Renumber Section 3 sections to match notebook execution order: Cell 3 (validation) → 3.1, Cell 4 (split) → 3.2, Cell 5 (pipeline) → 3.3. Supersedes the 3.1/3.2 assignment from Decision #29. | Keep 3.1=Split, 3.2=Validation (preserving the original letter-based logical order where split was "3A" = first topic) | The original 3A (Split) / 3B (Validation) letter ordering reflected a logical writing priority (introduce the split method first), but with sequential decimal numbers, having 3.2 appear before 3.1 in the document is confusing to a marker. Decimal numbers must reflect document/notebook order. The validation cell runs first (Cell 3), so it becomes 3.1; the split cell runs second (Cell 4), so it becomes 3.2. This makes the document read sequentially. | **Agent identified the ordering problem** | Yes | **Supersedes the 3.1/3.2 part of Decision #29.** Decision #24 note and Decision #25 note updated accordingly. |
